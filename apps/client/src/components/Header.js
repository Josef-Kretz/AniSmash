const Header = ({title}) => {
    return <header>
        {/* <img src={siteLogo} alt='site-logo' /> */}
        <h1>{title}</h1>
        <ul>
            <li>Login</li>
        </ul>
    </header>
}

export default Header